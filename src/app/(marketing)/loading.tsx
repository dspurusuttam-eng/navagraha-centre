import { LoadingPanel } from "@/components/feedback/loading-panel";

export default function MarketingLoading() {
  return (
    <LoadingPanel
      eyebrow="Marketing"
      title="Loading the next editorial surface."
      description="The public route is being prepared with the same calm pacing as the main experience."
    />
  );
}
